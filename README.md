# py_evolu
Python experiment for Evolu: a simple wrapper and interactive example for local SQLite data, relay sync, backup export, and mnemonic restore using the official Evolu TypeScript packages under the hood.
